#include <stdlib.h>
#include <player.h>
#include <arraylist.h>

Player* initPlayer(){
	Player* new_player = malloc(sizeof(Player));
	
	if (new_player == nullptr) return nullptr;

	new_player->units = a
}
